# 🧱 Minecraft AR Builder

Творческий мастер-класс, где мы превращаем комнату в строительную площадку Minecraft. С помощью жестов рук дети могут устанавливать блоки земли прямо в воздухе, соблюдая идеальную стыковку.

---

## 📋 Требования и зависимости

* **Python:** 3.11.9
* **Библиотеки:** 
* `mediapipe==0.10.11` (основа для распознавания рук)
* `cvzone==1.6.1` (утилиты для работы с координатами)
* `opencv-python` (обработка видео)
* `pygame` (звуковые эффекты)

**Установка:**

```bash
pip install -r requirements.txt

```

## 📂 Структура проекта

* `main.py` — **Код разработчика**. Здесь дети пишут игровую логику (всего ~15 строк).
* `builderLib.py` — **Игровой движок**. Служебный файл, который отвечает за работу с камерой, распознавание рук и математику наложения текстур.
* `asset/dirt.png` — Текстура блока (можно заменить на любую другую).

---

## ⚙️ Как это работает (Теория)

Этот проект знакомит учеников с двумя продвинутыми концепциями:

1. **Сетка (Snap to Grid):** Чтобы блоки не висели хаотично, мы делим экран на виртуальные квадраты (например, 40x40 пикселей).
* **Формула:** `snap_x = (cx // GRID_SIZE) * GRID_SIZE`.
* Это позволяет «примагничивать» блок к ближайшей ячейке сетки.
2. **Словарь для хранения мира:** Мы используем словарь `game.blocks = {}`. Ключом являются координаты `(x, y)`, а значением — логическое True. Это позволяет мгновенно сохранять и отрисовывать все постройки.
3. **Alpha-Matte (Очистка краев):** Код автоматически находит границы реального рисунка внутри PNG и обрезает пустые прозрачные пиксели, чтобы блоки соприкасались «шов в шов».

---

## 👨‍🏫 Методические подсказки (Teacher's Guide)

### ⏱ Стратегия проведения

Проект разделен на две части, чтобы дети не отвлекались на сложную математику:

1. **Выдать готовую библиотеку** `builderLib.py`: В ней спрятана работа с OpenCV и наложение текстур.
2. **Сфокусироваться на** `main.py`: Разобрать создание объекта `game`, получение кадра и логику обработки данных (`info`), которую возвращает библиотека.

### 1. План обсуждения

* **"Почему блоки в Minecraft не стоят криво?"** — Объясните, что весь мир игры — это сетка. Покажите на примере кода внутри библиотеки, как работает целочисленное деление `//` для создания сетки.
* **"Как работает память игры?"** — Расскажите, что `game.blocks` — это словарь, который хранит координаты всех поставленных кубов. Если нажать `C`, мы просто очищаем этот словарь.

### 2. Идеи для доработки (Задания ученикам)

* **Размер блоков:** Изменить `grid_size=40` при создании игры на `80` или `20`. Как изменится масштаб построек?
* **Инструмент «Ластик»:** Добавить условие: если расстояние между пальцами очень маленькое (например, `length < 20`), блок по этим координатам удаляется: `del game.blocks[info["pos"]]`.
* **Управление жестами:** Добавить проверку: если подняты обе руки (изменить `maxHands=1` на `2`), происходит полная очистка поля без использования клавиатуры.

### 3. Управление

* **Соединить указательный и большой пальцы:** Поставить блок.
* **Клавиша 'C' (Eng):** Очистить всё поле.
* **Клавиша 'Q' (Eng):** Выход.