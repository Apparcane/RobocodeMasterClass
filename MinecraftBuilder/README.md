# 🧱 Minecraft AR Builder

Творческий мастер-класс, где мы превращаем комнату в строительную площадку Minecraft. С помощью жестов рук дети могут устанавливать блоки земли прямо в воздухе, соблюдая идеальную стыковку.

---

## 📋 Требования и зависимости

* **Python:** 3.11.9
* **Библиотеки:** 
* `mediapipe==0.10.11` (основа для распознавания рук)
* `cvzone==1.6.1` (утилиты для работы с координатами)
* `opencv-python` (обработка видео)
* `pygame` (звуковые эффекты)

**Установка:**

```bash
pip install -r requirements.txt

```

## 📂 Структура проекта

* `main.py` — основной файл (код, который пишут дети).
* `asset/dirt.png` — текстура блока земли.
* *(В разработке)* `builderLib.py` — библиотека для выноса сложной логики отрисовки.

---

## ⚙️ Как это работает (Теория)

Этот проект знакомит учеников с двумя продвинутыми концепциями:

1. **Сетка (Snap to Grid):** Чтобы блоки не висели хаотично, мы делим экран на виртуальные квадраты (например, 40x40 пикселей).
* **Формула:** `snap_x = (cx // GRID_SIZE) * GRID_SIZE`.
* Это позволяет «примагничивать» блок к ближайшей ячейке сетки.
2. **Словарь для хранения мира:** Мы используем `blocks = {}`. Ключом являются координаты `(x, y)`, а значением — наличие блока. Это позволяет мгновенно проверять, занято ли место, прежде чем поставить новый блок.
3. **Alpha-Matte (Очистка краев):** Код автоматически находит границы реального рисунка внутри PNG и обрезает пустые прозрачные пиксели, чтобы блоки соприкасались «шов в шов».

---

## 👨‍🏫 Методические подсказки (Teacher's Guide)

### ⏱ Стратегия проведения

Так как проект находится в стадии разработки (весь код в `main.py`), для детей 9-12 лет рекомендуется:

1. **Выдать готовую функцию `overlay_block**`: Это самая сложная часть с математикой массивов NumPy.
2. **Сфокусироваться на цикле**: Написать вместе получение координат `cx, cy` и логику `if length < 45`.

### 1. План обсуждения

* **"Почему блоки в Minecraft не стоят криво?"** — Объясните, что весь мир игры — это сетка. Покажите на примере кода, как работает целочисленное деление `//`.
* **"Как работает память игры?"** — Расскажите, что переменная `blocks` хранит всё, что они построили. Если нажать `C`, память очищается.

### 2. Идеи для доработки (Задания ученикам)

* **Размер блоков:** Изменить `GRID_SIZE = 40` на `80` или `20`. Как это повлияет на сложность постройки?
* **Удаление блоков:** Попробуйте добавить логику: если расстояние между пальцами очень маленькое (например, `< 20`), блок по этим координатам удаляется из словаря (`del blocks[pos]`).
* **Управление:** Добавить кнопку «Очистить всё» не только на клавиатуре (`C`), но и на какой-нибудь жест.

### 3. Управление

* **Соединить пальцы:** Поставить блок.
* **Клавиша 'C' (Eng):** Очистить всё поле.
* **Клавиша 'Q' (Eng):** Выход.