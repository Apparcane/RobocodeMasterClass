# 💥 AR Creeper Game

Интерактивный мастер-класс, на котором мы создаем дополненную реальность с персонажем из Minecraft. Крипер привязывается к руке пользователя и «взрывается», если его ущипнуть.

---

## 📋 Требования и зависимости

* **Python:** 3.11.9
* **Библиотеки:** 
* `mediapipe==0.10.11` (основа для распознавания рук)
* `cvzone==1.6.1` (утилиты для работы с координатами)
* `opencv-python` (обработка видео)
* `pygame` (звуковые эффекты)

---

**Установка:**

```bash
pip install -r requirements.txt

```

## 📂 Структура проекта

* `main.py` — точка входа, цикл захвата видео.
* `creeperLib.py` — логика игры: состояния крипера, отрисовка и система частиц.
* `asset/` — ресурсы (текстура `creeper.png` и звук `boom.mp3`).

## ⚙️ Как это работает (Теория)

Проект демонстрирует три важные концепции программирования:

1. **Computer Vision (CV):** Мы используем нейросеть для поиска 21 точки на руке. Расстояние между большим (ID 4) и указательным (ID 8) пальцами используется как триггер «клика».
2. **Математика наложений:** Функция `draw_creeper` рассчитывает прозрачность (Alpha-канал), чтобы PNG-картинка корректно смешивалась с видеопотоком.
3. **Машина состояний (State Machine):** Крипер находится в одном из трёх состояний:
* `0 (Idle)`: Просто следует за рукой.
* `1 (Fuse)`: Шипит, мигает красным, готовится к взрыву.
* `2 (Boomed)`: Создает частицы и ждет, пока рука "сбросит" состояние.

---

## 👨‍🏫 Методические подсказки (Teacher's Guide)

### ⏱ Тайм-менеджмент и стратегия написания кода

Для детей **9–12 лет** рекомендуется использовать стратегию **"Драйвер и Модуль"**:

* **creeperLib.py (Модуль):** Выдайте этот файл детям в готовом виде. Объясните его содержимое "на пальцах": это мозг и тело нашего крипера, который умеет рисовать себя и взрываться.
* **main.py (Драйвер):** Этот файл дети должны **написать сами**. Он короткий, понятный и наглядно показывает, как мы подключаем библиотеку, включаем камеру и создаем игровой цикл.

> **Зачем это нужно?** Такой подход позволяет сфокусироваться на логике (захват видео, условия нажатия), а не на долгой и нудной печати сложных формул отрисовки и систем частиц.

### 1. Подготовка

Перед уроком проверьте, что камера видит руки ученика целиком. Лучший результат достигается при нейтральном фоне.

### 2. План обсуждения

* **Импорт своей библиотеки:** Обсудите с детьми, что `from creeperLib import CreeperGame` — это как подключение мода в Minecraft. Мы берем готовые функции и используем их.
* **Координаты :** Спросите, знают ли они, что экран — это большая карта с осями. Центр между пальцами (`cx`, `cy`) — это точка, где появится персонаж.

### 3. Идеи для доработки (Задания ученикам)

* **Таймер:** Изменить время до взрыва в строке `self.boom_time = now + 2.5`.
* **Визуал:** Изменить цвет или размер частиц в цикле `for _ in range(30)`.
* **Звук:** Настроить громкость взрыва через `set_volume()`.

### Почему это сработает:

1. **Успех в конце урока:** Дети успеют дописать `main.py` за 15–20 минут, и у них останется еще полчаса, чтобы «поиграть» с параметрами и поэкспериментировать.
2. **Понимание структуры:** Они поймут, что профессиональный код всегда делится на части (модули).

### ⚠️ Типичные проблемы

* **Ошибка размера ROI:** Если поднести руку вплотную к краю экрана, программа может закрыться. В коде предусмотрена базовая защита `max(0, ...)`, но лучше держать руку в центре кадра.
* **FPS:** Если видео тормозит, проверьте, не запущены ли тяжелые приложения (например, браузер с 50 вкладками).